services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: nbddb
      POSTGRES_USER: nbd
      POSTGRES_PASSWORD: nbdpassword
    ports:
      - 5444:5432
    volumes:
      - pg:/var/lib/postgresql/data
volumes:
  pg:
